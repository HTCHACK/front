export const SET_CARS = (state, cars) => {
  state.cars = cars;
};

export const SET_PRODUCT = (state, product) => {
  state.product = product;
};

export const SET_HOLD = (state, hold) => {
  state.hold = hold;
};
